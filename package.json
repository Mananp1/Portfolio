{
  "name": "portfolio-monorepo",
  "private": true,
  "engines": {
    "node": ">=18"
  },
  "scripts": {
    "build": "npm install --prefix client && npm run build --prefix client && npm install --prefix server && cp -r client/dist/* server/public/",
    "start": "npm start --prefix server"
  }
}
